apiVersion: v1
kind: Service
metadata:
  name: {{ include "kciss.fullname" . }}
  labels:
    {{- include "kciss.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kciss.selectorLabels" . | nindent 4 }}
